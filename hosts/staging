# Add each host to the [staging] group and to a "type" group such as [web] or [db].
# List each machine only once per [group], even if it will host multiple sites.

# Disabling host key checking is dangerous! Only use it on Kinsta!
kinsta_staging ansible_host=xxxSSH_IPxxx ansible_port=xxxSTAGING_SSH_PORTxxx

[web]
kinsta_staging

[staging]
kinsta_staging
